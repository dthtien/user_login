<%= simple_form_for [@post, @comment] do |f| %>
  <%= f.input :content %>
  <%= f.simple_fields_for :image do |image| %>
    <%= image.input :image_upload, as: :file %>
  <% end %>
  <%= f.submit class: "btn blue"%>
<% end %>