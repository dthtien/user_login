postModal = $('#post-modal');
if ( postModal.length > 1 ) {
  postModal.remove();
}
$('body').append("<%= j render('modal_form') %>");
postModal = $('#post-modal')
postModal.modal()
postModal.modal('open')