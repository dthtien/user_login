postModal = $('#post-modal');
if ( postModal.length > 1 ) {
  console.log('edit');
  postModal.remove();
}
$('body').append("<%= j render('modal_form', post: @post) %>");
postModal = $('#post-modal')
postModal.modal()
postModal.modal('open')